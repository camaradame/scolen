{% extends "EcoleFrontBundle::layout.html.twig" %}

{#{% block title %}EcoleFrontBundle:Ecole:index{% endblock %}#}
{% block stylesheets %}
    <link href="{{ asset('bower_components/jquery-ui/themes/redmond/jquery-ui.css') }}" rel="stylesheet" media="screen">
    <link href="{{ asset('bower_components/datatables/media/css/dataTables.bootstrap.css') }}" rel="stylesheet" media="screen">
{% endblock %}


{% block body_content %}
    <div class="content-box-large">
        <div class="panel-heading">
            <div class="panel-title">La liste des utilisateuers</div>
        </div>
        <div class="panel-body">
            <table cellpadding="0" cellspacing="0" border="0" class="table table-striped table-bordered" id="example">
                <thead>
                <tr>
                    <th>Usernme</th>
                    <th>Email</th>
                    <th>Role</th>
                    <th>Enabled</th>
                </tr>
                </thead>
                <tbody>
                {% for entity in entities %}
                    <tr class="gradeA">
                        <td>{{ entity.username }}</td>
                        <td>{{ entity.email }}</td>
                        <td>
                            <select name="" id="" style="height: 22px; font-size: 10px;">
                                {% for role in entity.roles %}
                                    <option value="">{{ role }}</option>
                                {% endfor %}
                            </select>
                        </td>
                        <td style="color: {{ entity.enabled ? "#00dd1c" : "maroon"}}">
                            {{ entity.enabled ? 'Oui' : 'Non' }}
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    <!-- jQuery UI -->
    <script src="{{ asset('bower_components/jquery-ui/jquery-ui.js') }}"></script>
    <script src="{{ asset('bower_components/datatables/media/js/jquery.dataTables.min.js') }}"></script>
    <script src="{{ asset('bower_components/datatables/media/js/dataTables.bootstrap.js') }}"></script>
    <script src="{{ asset('js/tables.js') }}"></script>
{% endblock %}
